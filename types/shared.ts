export type TProductCard = {
  id: string;
  name: string;
  price: number;
  image: string;
};

export type TProductCardProps = {
  title: string;
  price: number;
  description: string;
  image: string;
  id: string;
};
